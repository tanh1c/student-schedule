# ═══════════════════════════════════════════════════════
# Student Schedule API — Environment Variables
# Copy this file to .env and fill in your values
# ═══════════════════════════════════════════════════════

# Server
NODE_ENV=development
PORT=3001
CORS_ORIGIN=http://localhost:3000

# Security — REQUIRED in production!
# Must be exactly 64 hex characters (32 bytes for AES-256-GCM)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
CREDENTIALS_ENCRYPTION_KEY=

# Redis
# Format: redis://:password@host:port
# For local development without password:
REDIS_URL=redis://localhost:6379
# For Docker with password:
# REDIS_URL=redis://:mybk_redis_secret@redis:6379
REDIS_PASSWORD=mybk_redis_secret

# GitHub API (optional — for contributors page)
# GITHUB_TOKEN=ghp_your_token_here
